/** @type {import('tailwindcss').Config} */

export default {

    theme: {
        extend: {
            fontFamily: {
                'default': ['DM Sans', '-apple-system, BlinkMacSystemFont, “Segoe UI”, “Roboto”, “Oxygen”, “Ubuntu”, “Cantarell”, “Fira Sans”, “Droid Sans”, “Helvetica Neue”, sans-serif'],
            }
        }
    },
    content: [
        './*.html',
    ],
};
